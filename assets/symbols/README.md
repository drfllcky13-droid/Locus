# Diagram symbols

Original symbols drawn for Locus (nothing copied from other products). Each file is a 100 × 100 SVG centred on the origin, drawn in `currentColor`, seen from above; `index.json` gives each symbol's name and its default size on the ground in metres. Generated by hand-written code; edit the files directly.
