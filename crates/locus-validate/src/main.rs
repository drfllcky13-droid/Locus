//! Synthetic ground-truth scenes and the end-to-end accuracy suite (built in Phase 13).

fn main() {
    eprintln!("locus-validate: no validation scenarios yet (Phase 13)");
    std::process::exit(1);
}
